<%= form_with(model: quizz, local: true) do |form| %>
  <% if quizz.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quizz.errors.count, "error") %> prohibited this quizz from being saved:</h2>

      <ul>
      <% quizz.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :quizz_name %>
  </div>

  <div class="field">
    <%= form.label :time %>
    <%= form.text_field :time, id: :quizz_time %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.number_field :category_id, id: :quizz_category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
